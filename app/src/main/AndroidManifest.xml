<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.letsconnectfinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WebDev"></activity>
        <activity android:name=".ViewDomainsButton" />
        <activity android:name=".showEvent" />
        <activity android:name=".ML_AI" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".IOTPage" />
        <activity android:name=".ImageUpload" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AdminHomePage" />
        <activity android:name=".addEvent" />
        <activity android:name=".addDomain" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>